= link_to current_user do
  = gravatar_for current_user, size: 52

%h1= current_user.name
%span= link_to "view my profile", current_user
%span.micropost_counter= pluralize(current_user.microposts.count, "micropost")
